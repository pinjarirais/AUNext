import axios from "axios"

export  async function changePin(url) {
    const token = JSON.parse(localStorage.getItem("token"));


  return response
}

// http://localhost:8081/api/cardholders/validateOtp